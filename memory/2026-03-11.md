# 2026-03-11

- Initialized workspace bootstrap conversation.
- Installed `skills/self-improving-agent` via ClawHub.
- Created `.learnings/` with `LEARNINGS.md`, `ERRORS.md`, and `FEATURE_REQUESTS.md`.
- Enabled the `self-improvement` OpenClaw hook.
- Logged the first tool limitation: `clawhub install` rejects full URLs and needs a slug.
- User provided a new operating style for `SOUL.md`: concise, technical, mission-focused, CLI-first, with explicit safety boundaries around `.env`, `credentials/`, PII redaction, spending over `$50`, and quiet hours.
- Identity bootstrap updated: assistant name is `小虾米`, user should be addressed as `老大`, and Chinese is the default language.
